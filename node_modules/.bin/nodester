#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nodester-cli/bin/nodester.js" "$@"
  ret=$?
else 
  node  "$basedir/../nodester-cli/bin/nodester.js" "$@"
  ret=$?
fi
exit $ret
